{% extends 'base.html.twig' %}

{% block title %}EquipementInt{% endblock %}

{% block body %}
    <h1>EquipementInt</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ equipement_int.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ equipement_int.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_equipement_int_index') }}">back to list</a>

    <a href="{{ path('app_equipement_int_edit', {'id': equipement_int.id}) }}">edit</a>

    {{ include('equipement_int/_delete_form.html.twig') }}
{% endblock %}
