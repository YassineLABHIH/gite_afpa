{% extends 'base.html.twig' %}

{% block title %}EquipementInt index{% endblock %}

{% block body %}
    <h1>EquipementInt index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for equipement_int in equipement_ints %}
            <tr>
                <td>{{ equipement_int.id }}</td>
                <td>{{ equipement_int.name }}</td>
                <td>
                    <a href="{{ path('app_equipement_int_show', {'id': equipement_int.id}) }}">show</a>
                    <a href="{{ path('app_equipement_int_edit', {'id': equipement_int.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_equipement_int_new') }}">Create new</a>
{% endblock %}
