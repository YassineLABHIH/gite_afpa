{% extends 'base.html.twig' %}

{% block title %}EquipementExt{% endblock %}

{% block body %}
    <h1>EquipementExt</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ equipement_ext.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ equipement_ext.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_equipement_ext_index') }}">back to list</a>

    <a href="{{ path('app_equipement_ext_edit', {'id': equipement_ext.id}) }}">edit</a>

    {{ include('equipement_ext/_delete_form.html.twig') }}
{% endblock %}
